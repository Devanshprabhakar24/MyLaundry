{
  "name": "mylaundry-monorepo",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend"
  },
  "dependencies": {}
}